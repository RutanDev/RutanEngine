namespace Rutan::Scene::Systems
{
    // Some automatic functions later on

    // Movement: pos += velocity * dt
    
    // CameraController / PlayerController?

    void UpdateCameras(const entt::registry& registry);



}